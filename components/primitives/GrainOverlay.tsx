// Static, fixed, non-interactive noise texture — a single quiet nod to the
// "mysterious tech" reference mood. Pure CSS/SVG, no JS, no layout cost.
export default function GrainOverlay() {
  return <div className="grain-overlay" aria-hidden="true" />
}
